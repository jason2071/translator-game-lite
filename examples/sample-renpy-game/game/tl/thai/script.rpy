translate thai strings:

    # This pair already has a translation — the app imports it as "Edited".
    old "Guild"
    new "กิลด์"

    # This one has no `new` line yet — export will insert it.
    old "Master"
